---
config:
  layout: elk
---
flowchart TB
 subgraph Application["Application Layer (Event handling)"]
        Services["Application Services"]
  end
 subgraph Domain["Domain Layer"]
        Entities["Entities / Aggregates"]
  end
 subgraph ReactiveFrame["Holder power - From Duty"]
        Condition["Event"]
        Consequence["Consequence"]
  end
    Services --> Entities
    Condition --> Services
    Consequence --> Services
